language: cpp
compiler: gcc
dist: trusty
sudo: required
branches:
  only:
    - develop
addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake
      - cmake-data
script:
  - mkdir build && cd build && cmake .. && make
  - ./xzbuf-test